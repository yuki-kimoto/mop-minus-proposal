Object::Simple::Syntax

  Provide Object::Simple good syntax.

EXAMPLE

  # main.pl
  use Point3D;
  my $point = Point3D->new;
  print $point->x(0);
  my $x = $point->x;
  
  # Point3D.pm
  package Point3D {
    use Object::Simple::Syntax;
    
    extends Point;
    with Role1, Role2;
    
    has z = 0;
    
    sub clear ($self) {
      $self->z(0);
    }
  }
  
  # Point.pm
  package Point {
    use Object::Simple::Syntax;
    
    has x = 0;
    has y = 0;

    sub clear ($self) {
      $self->x(0);
      $self->y(0);
    }
  }

  # Role1.pm
  package Role1 {
    use Object::Simple::Syntax;

    sub foo {
      return 'foo';
    }
  }

  # Role2.pm
  package Role1 {
    use Object::Simple::Syntax;

    sub bar {
      return 'bar';
    }
  }

